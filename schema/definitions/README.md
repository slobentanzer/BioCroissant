# Definitions

**Purpose:** JSON Schema definitions for validating BioCroissant metadata.

**Current state:** Skeleton only; no schema files yet.

**Planned content:** JSON Schema definitions used by the validation layer.
