# Source

**Purpose:** Python source for the BioCroissant package.

**Current state:** Skeleton only; single package layout in place.

**Planned content:** Single package `biocroissant` with subpackages `core`, `validation`, `converters`, and `cli`, aligned with Croissant-style layout. Implementation to be added in a later phase.
