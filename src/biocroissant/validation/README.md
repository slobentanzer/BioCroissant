# validation

**Purpose:** Validate BioCroissant metadata against schema and optional semantic rules.

**Current state:** Skeleton only; no implementation yet.

**Planned content:** Schema validation (JSON Schema); optional semantic checks (e.g. required fields, references). Used by the `validate` CLI subcommand.
