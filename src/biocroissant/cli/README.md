# cli

**Purpose:** Command-line interface for validate, convert, and describe.

**Current state:** Stub only; `main` prints usage and exits. No real validation or conversion.

**Planned content:** Subcommands: `validate` (metadata path), `convert` (direction, input, output, options), `describe` (metadata path). Implementation will be wired to core, validation, and converters when added.
