# core

**Purpose:** Parse Bio-Croissant JSON-LD and load schema/context.

**Current state:** Skeleton only; no implementation yet.

**Planned content:** Parser for recordSet, distribution, and field extraction; schema loading from `schema/`; optional small in-memory models for metadata. Will be implemented (or ported) in a later phase.
